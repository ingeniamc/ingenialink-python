[package]
name = "telemetry"
version = "0.1.0"
edition = "2024"

[lib]
name = "telemetry"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { workspace = true, features = ["extension-module", "abi3-py39"] }

[build-dependencies]
cc = "1.2"

[lints]
workspace = true

[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "eoe-stack"
version = "0.1.0"
description = "Userspace smoltcp UDP/IPv4 stack over EoE Ethernet frames"
requires-python = ">=3.9"
